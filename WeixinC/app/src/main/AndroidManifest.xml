<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="forever.zybelieve.com.weixinc">

    <uses-permission android:name="android.hardware.sensor.accelerometer" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="Bean.StorageApplication"
        android:allowBackup="true"
        android:icon="@drawable/lu1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/lu2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".SignIn" />
        <activity
            android:name=".MainCustomerUser"
            android:label="@string/title_activity_main_customer_user" />
        <activity android:name=".NavigationHome" />
        <activity
            android:name=".NavigationDashboard"
            android:label="@string/title_activity_navigation_dashboard" />
        <activity android:name=".NavigationNotifications" />
        <activity android:name=".NavigationUser" />
        <activity
            android:name=".ChatActivity"
            android:label="@string/title_activity_chat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".OnselfActivity" />
        <activity android:name=".SharkActivity" />
        <activity android:name=".ImageMax" />
        <!-- android:theme="@style/MyDialogStyleBottom" -->
        <activity android:name=".AddFreindsActivity"></activity>
    </application>

</manifest>